# This machine sets NODE_ENV=production globally, which makes npm silently omit
# devDependencies on every install. That strips Tailwind (a devDependency) and breaks
# the build. Pin dev deps on so `npm install` is deterministic here.
include=dev
