import WhotTable from "@/components/games/WhotTable";

export const metadata = {
  title: "Whot",
  description: "Staked Whot tables settled in MVR on Arc.",
};

export default function WhotPage() {
  return <WhotTable />;
}
